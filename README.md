# Lancer le pipeline via Docker Compose

1. Construire l'image et démarrer le conteneur :

   docker-compose up --build

2. Les fichiers générés (CSV et kpi.json) se trouvent dans le dossier :

   ./shared_out

3. Variables configurables via le fichier .env
